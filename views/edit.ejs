<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= player.name %> Stats</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>Edit Stats for <%= player.name %></h1>
    </header>

    <form action="/teams/<%= player._id %>?_method=PUT" method="POST">
        <div class="form-group">
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= player.name %>" required>
        </div>

        <div class="form-group">
            <label for="position">Position:</label>
            <input type="text" id="position" name="position" value="<%= player.position %>">
        </div>

        <div class="form-group">
            <label for="points">Points:</label>
            <input type="number" id="points" name="points" value="<%= player.points %>">
        </div>

        <div class="form-group">
            <label for="assists">Assists:</label>
            <input type="number" id="assists" name="assists" value="<%= player.assists %>">
        </div>

        <div class="form-group">
            <label for="rebounds">Rebounds:</label>
            <input type="number" id="rebounds" name="rebounds" value="<%= player.rebounds %>">
        </div>

        <button type="submit" class="btn">Update Player</button>
    </form>

    <a href="/teams/<%= player._id %>" class="btn">Cancel</a>

    <footer>
        <p>&copy; <%= new Date().getFullYear() %> Knicks Stats App</p>
    </footer>
</body>
</html>
